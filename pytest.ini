[pytest]
pythonpath = backend
DJANGO_SETTINGS_MODULE = config.settings_test
python_files = test_*.py
addopts = -ra -v --tb=short --showlocals --strict-markers -m "not postgres"
markers = postgres: tests that require PostgreSQL
